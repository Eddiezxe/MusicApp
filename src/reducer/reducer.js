const initialState = {
    userInfo: {},
}

const reducer = (prevSate, action) =>{
    //action -> type, [data]
    switch(action.type){
        
        default:
            return prevSate; // neu ko co gi xay ra tra ve data cu
    }
}
export default reducer
export {initialState}
